# Rust callers — `tokio-stream-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `tokio-stream` | `tokio_stream` | 62 | 13 | 12 | 15 |

## `tokio-stream`

### Inbound symbols (13 distinct, 62 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `tokio-stream`.

#### `tokio_stream::StreamExt` — 29 site(s) (9 codex, 20 other, 9 caller crates)

- ✅ `codex-kotlin/codex-cloud-tasks` `src/lib.rs:818` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/app.rs:621` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/cwd_prompt.rs:24` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/external_agent_config_migration.rs:27` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/model_migration.rs:23` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/onboarding/onboarding_screen.rs:494` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/resume_picker.rs:36` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/tui/event_stream.rs:307` — `use tokio_stream::StreamExt`
- ✅ `codex-kotlin/codex-tui` `src/update_prompt.rs:28` — `use tokio_stream::StreamExt`
- `axum-kotlin/axum` `src/response/sse.rs:644` — `use tokio_stream::StreamExt as _`
- `rama-http-kotlin/rama-http` `src/service/web/endpoint/extract/body/json_lines.rs:45` — `use tokio_stream::StreamExt as _`
- `rama-http-kotlin/rama-http` `src/service/web/endpoint/response/sse.rs:81` — `use tokio_stream::StreamExt as _`
- _… and 17 more site(s)._

#### `tokio_stream::Stream` — 18 site(s) (2 codex, 16 other, 5 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/tui.rs:38` — `use tokio_stream::Stream`
- ✅ `codex-kotlin/codex-tui` `src/tui/event_stream.rs:31` — `use tokio_stream::Stream`
- `libwebrtc-kotlin/livekit-runtime` `src/tokio.rs:24` — `pub_use tokio_stream::Stream`
- `tokio-kotlin/tokio` `tests/rt_basic.rs:56` — `use tokio_stream::Stream`
- `tokio-kotlin/tokio` `tests/support/mpsc_stream.rs:6` — `use tokio_stream::Stream`
- `tokio-kotlin/tokio` `tests/tcp_accept.rs:50` — `use tokio_stream::Stream`
- `tokio-test-kotlin/tokio-test` `src/task.rs:34` — `use tokio_stream::Stream`
- `tokio-test-kotlin/tokio-test` `tests/task.rs:3` — `use tokio_stream::Stream`
- `tonic-kotlin/tonic` `src/client/grpc.rs:17` — `use tokio_stream::Stream`
- `tonic-kotlin/tonic` `src/codec/decode.rs:15` — `use tokio_stream::Stream`
- `tonic-kotlin/tonic` `src/codec/encode.rs:14` — `use tokio_stream::Stream`
- `tonic-kotlin/tonic` `src/request.rs:14` — `use tokio_stream::Stream`
- _… and 6 more site(s)._

#### `tokio_stream::wrappers::UnboundedReceiverStream` — 2 site(s) (1 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/resume_picker.rs:37` — `use tokio_stream::wrappers::UnboundedReceiverStream`
- `tokio-test-kotlin/tokio-test` `src/io.rs:24` — `use tokio_stream::wrappers::UnboundedReceiverStream`

#### `tokio_stream::wrappers::BroadcastStream` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/tui/event_stream.rs:32` — `use tokio_stream::wrappers::BroadcastStream`

#### `tokio_stream::wrappers::WatchStream` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/tui/event_stream.rs:33` — `use tokio_stream::wrappers::WatchStream`

#### `tokio_stream::wrappers::errors::BroadcastStreamRecvError` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-tui` `src/tui/event_stream.rs:34` — `use tokio_stream::wrappers::errors::BroadcastStreamRecvError`

#### `tokio_stream::wrappers::ReceiverStream` — 3 site(s) (0 codex, 3 other, 1 caller crates)

- `rmcp-kotlin/rmcp` `src/handler/client/progress.rs:5` — `use tokio_stream::wrappers::ReceiverStream`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_server/session/local.rs:14` — `use tokio_stream::wrappers::ReceiverStream`
- `rmcp-kotlin/rmcp` `src/transport/streamable_http_server/tower.rs:8` — `use tokio_stream::wrappers::ReceiverStream`

#### `tokio_stream::wrappers::TcpListenerStream` — 2 site(s) (0 codex, 2 other, 2 caller crates)

- `opentelemetry-otlp-kotlin/opentelemetry-otlp` `tests/smoke.rs:11` — `use tokio_stream::wrappers::TcpListenerStream`
- `tonic-kotlin/tonic` `src/transport/server/incoming.rs:10` — `use tokio_stream::wrappers::TcpListenerStream`

#### `tokio_stream` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tonic-kotlin/tonic` `src/codegen.rs:4` — `pub_use tokio_stream`

#### `tokio_stream::Once` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tokio-util-kotlin/tokio-util` `src/either.rs:219` — `use tokio_stream::Once`

#### `tokio_stream::adapters::Fuse` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tonic-kotlin/tonic` `src/codec/encode.rs:14` — `use tokio_stream::adapters::Fuse`

#### `tokio_stream::iter` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tokio-util-kotlin/tokio-util` `tests/io_stream_reader.rs:5` — `use tokio_stream::iter`

#### `tokio_stream::once` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tokio-util-kotlin/tokio-util` `src/either.rs:219` — `use tokio_stream::once`
