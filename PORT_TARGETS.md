# Port targets — `tokio-stream-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `tokio-stream` that codex-rs uses, and therefore the minimum subset that `tokio-stream-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-157] (rank `31`, tier `direct`, status `To Do`)

**Cargo package name**: `tokio-stream`
**Rust module name (in `use` statements)**: `tokio_stream`

## Summary

- **6** distinct symbols imported by codex-rs
- **15** total `use` statements
- **10** codex source files affected
- **2** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `tokio_stream::StreamExt`

- **9 `use` site(s)** across **2 codex member crate(s)**: `codex-cloud-tasks`, `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `cloud-tasks/src/lib.rs:818` (`codex-cloud-tasks`) — `use tokio_stream::StreamExt`
- `tui/src/app.rs:621` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/cwd_prompt.rs:24` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/external_agent_config_migration.rs:27` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/model_migration.rs:23` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/onboarding/onboarding_screen.rs:494` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/resume_picker.rs:36` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/tui/event_stream.rs:307` (`codex-tui`) — `use tokio_stream::StreamExt`
- `tui/src/update_prompt.rs:28` (`codex-tui`) — `use tokio_stream::StreamExt`

### `tokio_stream::Stream`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/tui.rs:38` (`codex-tui`) — `use tokio_stream::Stream`
- `tui/src/tui/event_stream.rs:31` (`codex-tui`) — `use tokio_stream::Stream`

### `tokio_stream::wrappers::UnboundedReceiverStream`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/resume_picker.rs:37` (`codex-tui`) — `use tokio_stream::wrappers::UnboundedReceiverStream`

### `tokio_stream::wrappers::BroadcastStream`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/tui/event_stream.rs:32` (`codex-tui`) — `use tokio_stream::wrappers::BroadcastStream`

### `tokio_stream::wrappers::WatchStream`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/tui/event_stream.rs:33` (`codex-tui`) — `use tokio_stream::wrappers::WatchStream`

### `tokio_stream::wrappers::errors::BroadcastStreamRecvError`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/tui/event_stream.rs:34` (`codex-tui`) — `use tokio_stream::wrappers::errors::BroadcastStreamRecvError`
